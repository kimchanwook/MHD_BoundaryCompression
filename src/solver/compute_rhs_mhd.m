function state = compute_rhs_mhd(state, ~, ~, dt)
%COMPUTE_RHS_MHD Temporary placeholder update.
% For the architectural starter version, the interior state is left unchanged.
% The only evolution currently comes from imposed boundary velocities. This is
% deliberate until the full conservative flux update is added.
    %#ok<INUSD>
    state = state;
end
